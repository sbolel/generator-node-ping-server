{
  "name": "generator-ping-server",
  "version": "0.1.5",
  "description": "Starter template for pinging server",
  "main": "server/app.js",
  "engines": {
    "node": ">=5.6.0"
  },
  "scripts": {
    "start": "node ./bin/www",
    "debug": "DEBUG=* nodemon ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbolel/generator-node-ping-server.git"
  },
  "keywords": [
    "npm",
    "node",
    "ping",
    "starter",
    "template"
  ],
  "author": "Sinan Bolel <sinanbolel@gmail.com>",
  "license": "MIT",
  "homepage": "https://github.com/sbolel/generator-node-ping-server",
  "dependencies": {
    "debug": "^2.2.0",
    "express": "^4.13.4"
  },
  "devDependencies": {
    "nodemon": "^1.9.2"
  }
}
